# Copied verifier — keep in sync

These files are **verbatim copies** from `realizse-platform-ui/src/lib/` at commit
`6717ac81e17857bb927d19f5f1b35f2ee0b9629b`, so this public tool runs exactly the same
checks as the platform app:

- `credential-verifier.ts`
- `credential-verifier.test.ts`
- `credential-issuer.ts`
- `response-utils.ts`

Do not edit them here. To update, copy the files again from the platform repo, update
the commit above, and run `npm test`.
